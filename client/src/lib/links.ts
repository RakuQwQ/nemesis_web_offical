// Shared external links for the NEMESIS site.
// Edit this file in one place to update links across the entire site.

/** The Discord invite code (the part after `discord.gg/`). */
export const DISCORD_INVITE_CODE = 'VdvBegG6ax';

/** Display value (without protocol), used for showing the link as text. */
export const DISCORD_INVITE_DISPLAY = `discord.gg/${DISCORD_INVITE_CODE}`;

/** Full Discord invite URL — use this for `href` attributes. */
export const DISCORD_INVITE_URL = `https://${DISCORD_INVITE_DISPLAY}`;

/** Contact email used in Join section / footer. */
export const CONTACT_EMAIL = 'contact@nemesis.wiki';

/** Minecraft server IP shown in the Join section. */
export const MC_SERVER_IP = 'mc.nemesis.wiki';
